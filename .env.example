# Copy this file to .env and customize before running `docker compose up -d`.

# SECURITY: replace with your real public + LAN vhost URLs (must match the
# vhost blocks you add to your own Caddyfile — see deploy/DEPLOY.md).
# Comma-separated, no spaces.
ALLOWED_ORIGINS=https://cogito.example.com,https://cogito.home.arpa
